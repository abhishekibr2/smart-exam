import React from 'react'
import EmailTemplate from '@/components/Admin/Settings/EmailTemplate'

const page = () => {
    return (
        <div>
            <EmailTemplate activeKey={''} />
        </div>
    )
}

export default page
